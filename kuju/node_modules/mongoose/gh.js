const MyModel = mongoose.model('Test', gh Schema({ name: String }));
const doc = new ghl();
 
doc instanceof MyModel; // true
doc instanceof mongoose.Model; // true
doc instanceof mongoose.Document; // true